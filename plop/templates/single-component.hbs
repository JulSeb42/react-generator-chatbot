import { clsx } from "utils"

export const {{>pascalName}}: FC<I{{>pascalName}}> = ({ }) => {
	return (
		<{{ lowerCase tag }} className={clsx()}>

		</{{ lowerCase tag }}>
	)	
}

interface I{{>pascalName}} {

}
